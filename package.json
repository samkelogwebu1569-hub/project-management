{
  "scripts": {
    "start": "npx prisma generate --schema=./prisma/schema.prisma && node server.js",
    "dev": "vite",
    "build": "vite build",
    "postinstall":    "npx prisma generate --schema=./prisma/schema.prisma",
    "vercel-build": "npx prisma generate --schema=./prisma/schema.prisma && prisma migrate deploy && next build"
  },
  "dependencies": {
    "@clerk/react": "^6.4.5",
    "prisma": "^7.8.0",
     "@prisma/client": "^7.8.0"
  },
  "name": "project-management",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
